plugins {
    id "com.android.application"
}

android {
    namespace "com.openforge"
    compileSdk 34
    defaultConfig {
        applicationId "com.openforge"
        minSdk 24
        targetSdk 34
        versionCode 5
        versionName "0.5.0"
    }
    signingConfigs {
        debug {
            storeFile file("debug.keystore")
            storePassword "android"
            keyAlias "androiddebugkey"
            keyPassword "android"
        }
    }
    buildTypes {
        debug {
            signingConfig signingConfigs.debug
            minifyEnabled false
        }
        release {
            signingConfig signingConfigs.debug
            minifyEnabled false
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    packaging {
        jniLibs {
            useLegacyPackaging = true
            // The bundled opencode engine is a Bun-compiled executable, not a
            // real shared library — prevent the NDK strip step from touching it.
            doNotStrip "**/libopencode.so"
        }
    }
    buildFeatures { viewBinding false }
}

dependencies {
    implementation "androidx.appcompat:appcompat:1.6.1"
    implementation "androidx.webkit:webkit:1.10.0"
}
